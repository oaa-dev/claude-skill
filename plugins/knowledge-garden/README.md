# Knowledge Garden

Project knowledge management for Laravel and Next.js. Module indexing, dynamic schemas, knowledge capture, insights, and pruning.

## Components

| Type | Count | Description |
|------|-------|-------------|
| Agents | 1 | Learnings researcher |
| Commands | 3 | generate-module-docs, knowledge-insights, knowledge-prune |
| Skills | 5 | setup, module-docs, knowledge-docs, knowledge-insights, knowledge-prune |

## Quick Start

1. Install the plugin
2. Run `/setup` to detect your stack and generate a schema
3. Run `/generate-module-docs` to index your project modules
4. Use `/compound` (knowledge-docs skill) after solving problems to capture learnings
5. Run `/knowledge-insights` to see patterns across your knowledge base
6. Run `/knowledge-prune` periodically to clean up stale docs

## Skills

### setup

Interactive setup that detects your stack (Laravel, Next.js, or both), generates a `docs/knowledge/schema.yaml` with stack-appropriate enums, and creates the knowledge directory structure.

### module-docs

Generates per-module markdown files in `docs/knowledge/modules/`. Uses a reference-tracing approach: starts from module anchors (models for Laravel, route segments for Next.js) and greps the entire project to find everything connected.

### knowledge-docs

Captures solved problems as categorized documentation with YAML frontmatter. Captures learnings with dynamic schema support and stack-agnostic templates.

### knowledge-insights

Analyzes the knowledge base to surface patterns: top problem types, most affected modules, common root causes, severity distribution, monthly trends, and hotspot modules.

### knowledge-prune

Scores documented solutions on staleness (age, severity, framework version, pattern links, module existence) and presents candidates for archiving, deletion, or retention.

## Agents

### learnings-researcher

Searches `docs/knowledge/solutions/` for relevant past solutions by frontmatter metadata. Uses grep-first filtering for efficiency. Invoke before implementing features or fixing problems to surface institutional knowledge.

## Commands

| Command | Description |
|---------|-------------|
| `/generate-module-docs` | Scan project and generate module documentation |
| `/knowledge-insights` | Analyze knowledge base and show patterns |
| `/knowledge-prune` | Flag stale learnings for cleanup |

## Directory Structure (Generated)

After setup, your project will have:

```
docs/knowledge/
├── schema.yaml          # Stack-specific schema (generated by setup)
├── modules/             # Module docs (generated by module-docs)
│   ├── user.md
│   ├── order.md
│   └── ...
├── solutions/           # Problem docs (created by knowledge-docs)
│   ├── performance-issues/
│   ├── runtime-errors/
│   └── ...
├── patterns/
│   └── critical-patterns.md
└── archive/             # Archived stale docs (by knowledge-prune)
```

## License

MIT
